###### CONFIG (mara barka)
git config --global user.name "name"
git config --global user.email "email"

###### BRANCHES 
git branch name // create branch "name"
git checkout name // switch to branch "name"
git status // check el branch eli tekhdem fih

kif tbadel el branch yetbadel contenu el dossier el akher commit saret fel 
branch edhika

###### LOCAL
git init (awel marra bark)
git add "path" / "file" / .  // el point = current directory
git commit
git staus // tchouf chnoua zedt fi enehi branch

git restore "file" --staged // tfasakh fichier,dossier mel commit
(moch sûr)

git commit 

###### REMOTE
git remote add origin https://github.com/BelkaDev/professional-network-pidev.git
// awel mara bark
git pull origin "essm branch" --allow-unrelated-histories 
// thabet eli fi branch 
get push origin "essm branch"
git rm "file" // tfasakh fichier mel git repo